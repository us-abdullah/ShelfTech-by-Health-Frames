# Copy this file to .env and fill in your values.

# For detection (item boxing): use either a Google Gemini key or a Dedalus key (dsk-...).
# Dedalus: https://dedaluslabs.ai – use your credit for detection.
VITE_GEMINI_API_KEY=YOUR_GEMINI_OR_DEDALUS_API_KEY

# Optional: Dedalus key for voice "Ask about this product" only. If set, voice Q&A uses Dedalus (not Gemini).
# If unset, voice uses VITE_GEMINI_API_KEY (Dedalus when key is dsk-..., else Gemini).
# VITE_DEDALUS_VOICE_API_KEY=dsk-your-dedalus-key

# Dedalus model for voice Q&A only (other features use Gemini 2.0 Flash). Default: mistralai/mistral-small.
# Use a different model so voice and detection use two models on one API key. Examples: meta/llama-3.2-3b-instruct, deepseek/deepseek-chat
# VITE_DEDALUS_VOICE_MODEL=mistralai/mistral-small

# For Gemini Live (voice + vision): must be a Google API key. Only needed if you use "Start Gemini Live".
# If you use Dedalus for detection, add a Google key here for Live: https://aistudio.google.com/apikey
# VITE_GEMINI_LIVE_API_KEY=YOUR_GOOGLE_API_KEY

# Optional: GrocerEye YOLO server for branded item boxing (https://github.com/bhimar/GrocerEye)
# Run grocer-eye-server and set this to use YOLO detection instead of Gemini for boxes.
# VITE_GROCEREEYE_API_URL=http://localhost:5000

# Optional: OpenClaw (Claw bot) for "execute" actions (add to list, search, etc.)
# See VisionClaw README for OpenClaw setup. Use your Mac's Bonjour hostname.
VITE_OPENCLAW_HOST=http://YOUR_MAC_HOSTNAME.local
VITE_OPENCLAW_PORT=18789
VITE_OPENCLAW_GATEWAY_TOKEN=YOUR_OPENCLAW_GATEWAY_TOKEN
